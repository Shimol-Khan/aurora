<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to image_tag('brand.png', :class=>"h-8 w-8 me-2"), root_path %>
    <%= link_to "Aurora", root_path, :class=>"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Update Account", edit_user_registration_path, :class=>"btn btn-primary me-2"%>
          </li>
          <li class="nav-item">
            <%= link_to "Log Out", destroy_user_session_path, method: :delete, :class=>"btn btn-danger"%>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, :class=>"btn btn-primary"%>
          </li>
          <li class="nav-item">
            <%= link_to "Sign Up", new_user_registration_path, :class=>"ms-2 btn btn-info"%>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>